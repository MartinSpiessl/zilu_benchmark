names= x y z
precondition= x>0 || y>0 || z>0
loopcondition=
loop=Depth(1); if (x>0) x++; else { Depth(2); if (y>0) y++; else { Depth(3); if(z>0) z++;}}
postcondition=x>0 || y>0 || z>0
learners=linear
